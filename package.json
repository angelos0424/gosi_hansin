{
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "npm run generate:hwp && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "start": "vite preview --host 0.0.0.0 --port ${PORT:-4173}",
    "extract": "python3 scripts/extract_questions.py",
    "generate:hwp": "node scripts/generate_hwp_html.mjs > /dev/null 2> /dev/null"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "^5.4.14"
  },
  "devDependencies": {
    "@ohah/hwpjs": "^0.1.0-rc.10",
    "playwright": "^1.60.0"
  }
}
